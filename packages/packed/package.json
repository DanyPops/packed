{
  "name": "@danypops/packed",
  "version": "0.5.2",
  "description": "Host-neutral contracts and persistence primitives for agent package composition",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && bun build src/public/atomic-json.ts --outdir dist --target node --format esm --minify && bunx tsc -p tsconfig.public.json",
    "test": "bun run build && bun test test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@danypops/vehicle-core": "^0.6.1",
    "@danypops/vehicle-server": "^0.7.0"
  },
  "devDependencies": {},
  "exports": {
    "./atomic-json": {
      "types": "./dist/atomic-json.d.ts",
      "import": "./dist/atomic-json.js"
    }
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DanyPops/packed.git",
    "directory": "packages/packed"
  },
  "keywords": ["agent-tools", "package-manager"],
  "files": ["src", "dist", "README.md"]
}
